name: Build Xero KDE ISO

on:
  schedule:
    - cron: "0 0 * * 6"
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 60

    permissions:
      contents: write

    container:
      image: archlinux:latest
      options: --privileged

    steps:
      - uses: actions/checkout@v2

      - name: Set current date as environment variable
        run: echo "DATE=`date +'%Y.%m.%d'`" >>$GITHUB_ENV
        
      - name: Create temporary build directory
        run: mkdir -p /tmp/$DATE/airootfs/root/
        
      - name: Update packages and install archiso
        run: pacman -Syu --noconfirm archiso mkinitcpio-archiso git squashfs-tools

      - name: Copy profile to build directory
        run: cp -r ./archiso /tmp/$DATE
        
      - name: Build ISO
        run: bash build.sh
        
      - name: Upload iso to sourceforge
        run: |
          rsync --delete -vaP --stats \
              -e ssh /tmp/$DATE thecode-breaker@frs.sourceforge.net:/home/frs/project/xerolinux-iso-actions/kde-iso
              
